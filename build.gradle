plugins {
    id 'com.github.node-gradle.node' version '2.2.4'
}

node {
    download true
    version '14.5.0'
}

npmInstall.args = ['nodemon', 'express']

task nodeRun(type: NpmTask) {
    description 'Runs NodeJS server instance.'
    dependsOn npmInstall
    args = ['start']
}