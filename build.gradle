plugins {
    id 'com.github.node-gradle.node' version '2.2.4'
}

node {
    download true
    version '14.5.0'
}

npm_install.args = ['nodemon', 'express']

task npmStart(type: NpmTask) {
    args = ['start']
}